# Copy to .env (in frontend/) to override. Vite bakes these at BUILD time;
# never put secrets here - the browser only talks to our backend.

# Base URL of the backend API. Leave empty for same-origin deployments
# (Vite dev proxy, or nginx serving /api). Example for a split deployment:
#VITE_API_BASE_URL=http://localhost:8000
